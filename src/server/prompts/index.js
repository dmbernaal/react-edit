const { SAFETY_RULE } = require('./safety');
const { PROMPT_TEMPLATES } = require('./templates');
const { ADD_PROMPT_TEMPLATES } = require('./addTemplates');

module.exports = {
  SAFETY_RULE,
  PROMPT_TEMPLATES,
  ADD_PROMPT_TEMPLATES
};

